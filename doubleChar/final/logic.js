//function statement named doubleChar with param str
function doubleChar(str){
    //return str property for split method map method create a function
    //on each element then element plus element then join method 
        return str.split('').map(c => c + c).join('');
    }