//function statment named stringToArray with param string
function stringToArray(string){
    // return string.split(' ');[spliting every letter with the space patern]
        return string.split(' ');
    }